DOMAIN=example.workbright-qa.dev
EMAIL=test-account@workbright.com
PASSWORD=test-password
# Secret matched by the Cloudflare WAF "Skip" custom rule to bypass the bot check.
# Generate with: openssl rand -hex 32
CF_BYPASS_TOKEN=